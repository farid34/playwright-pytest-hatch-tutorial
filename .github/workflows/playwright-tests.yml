name: Playwright E2E Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: 📦 Install Hatch
      run: pip install hatch
    
    - name: 🔧 Install dependencies
      run: hatch env create
    
    - name: 🌐 Install Playwright browsers
      run: hatch run install-browsers
    
    - name: 🧪 Run tests
      run: hatch run test --headed=false
    
    - name: 📤 Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: test-results/
        retention-days: 7